---
title: Input
description: Mouse, touch and pointer lock input for cameras.
nav: 60
---

Some components can be controlled by the user, like [`PanTilt`](/aim/pan-tilt). They expose named **axes** (`pan`, `tilt`), and an [`<InputController>`](/input/input-controller) placed inside them maps mouse and touch drags onto those axes.

```tsx
import { Aim, InputController } from '@kvvasuu/klipp/react';

<VirtualCamera name="look-around" priority={10}>
  <Aim.PanTilt>
    <InputController
      mouseButtons={{ right: { axes: { x: 'pan', y: 'tilt' } } }}
      touches={{ one: { axes: { x: 'pan', y: 'tilt' } } }}
    />
  </Aim.PanTilt>
</VirtualCamera>
```

This reads as: dragging with the right mouse button, or with one finger, moves `pan` horizontally and `tilt` vertically.

Each axis is an [`InputAxis`](/input/input-axis), a value with limits, wrapping, damping and optional recentering. You can also drive an axis from your own code, for example in a cutscene.
